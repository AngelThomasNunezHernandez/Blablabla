version: '3.9'

services:
  sonareplica:
    image: python:3.12-slim
    container_name: sonareplica-web
    working_dir: /app
    command: sh -c "python app/server.py"
    volumes:
      - ./:/app
    ports:
      - "3000:3000"
