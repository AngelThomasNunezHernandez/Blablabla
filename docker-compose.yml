version: '3.9'

services:
  frontend:
    image: python:3.12-slim
    container_name: sonareplica-frontend
    working_dir: /app
    command: sh -c "python -m http.server 3000 --directory web"
    volumes:
      - ./:/app
    ports:
      - "3000:3000"
